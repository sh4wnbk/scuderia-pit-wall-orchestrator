# Bob's Memory Notes

## Git Operations
- **IMPORTANT**: Use the WSL terminal for all git operations, not the chat window or Source Control panel.
  - This ensures proper integration with the WSL environment where the repository is located
  - Git commands should be executed via `execute_command` tool when needed
  - Avoid using VS Code's built-in Source Control panel for git operations in this workspace